---
import { Image } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import Planocopy from '../assets/planocopy.jpg';
import logoreta4 from '../assets/logoreta4.png';
import autito from "../assets/autito.gif";
import bus from "../assets/bus.gif"


---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href={logoreta4.src} />

</head>
<body>
    <Layout title="Ubicación Reta">
    <main>
          
        <div class="transporte">
            <span><h2 >Transportes</h2></span>
            <div class="item1">
            
                
                
                <p> En transporte particular desde cualquier punto del país tomando la ruta 3, acceso ruta provincial 72</p>
                <br>
                <br>
                <br>
                <p>Una sola empresa <b>Reta-Tur</b> ofrece servicio directo al pueblo. Parte desde Buenos Aires: San Martín, Morón, San Justo. El viaje dura aproximadamente 8 horas por RN 3 empalme RP 72.
                    <br>
                    <br>
                    <br>
                *** Otro modo de llegar es desde Tres Arroyos con un <b>colectivo zonal llamado 'La Dorreguense'</b>. Tiene horarios fijos que se pueden consultar. Además funcionan remises desde localidades cercanas (Copetonas, Oriente, Claromecó).
                </p>
            </div>
        </div>
        
        <div class="plano">
            <div class="item2 ">
                <Image src={Planocopy} alt="Plano ciudad de Reta" id="myImage">
              </div>
              <!-- Modal para mostrar la imagen a pantalla completa -->
              <div id="myModal" class="modal">
                <span id="closeModal" class="close">&times;</span>
                <Image id="modalImage" src={Planocopy} alt="Imagen ampliada">
              </div>
              
              
        </div>
    </div>
</main>
</Layout>
</body>
</html>

<style>

    .transporte{
        max-width: 100vw;
        height: 60vh;
        border-radius: 8px;
        background-color: white;
        display: flex;
        flex-direction: column;
        
        align-items: center;
        align-content: center;
        justify-content: center;
        margin-top: 4vw;
        letter-spacing: 2px;
    }

    .item1{
        display: flex;
        align-items: left;
        align-content: left;
        justify-content: left;
        flex-direction: column;
        padding: 4vw;
        max-width: 60vw;
        text-align: justify; /* Centra el texto dentro del enlace */
        line-height: 1.5;
        font-family: "roboto", sans-serif;
        color: var(--primario);
    }


    .item2{
        display: flex;
        align-items: center;
        align-content: center;
        justify-content: center;
        flex-direction: column;
        font-family: "roboto", sans-serif;
        color: var(--primario);
       

    }

    .item2 img{
        cursor: pointer; /* Cursor de mano para indicar que se puede hacer clic */
        transition: transform 0.3s ease; /* Transición suave */
        height: auto;        
        max-width: 50vw;
        border-radius: 8px;
        
    }

    .item2 img:hover{
        box-shadow: 0 4px 8px var(--gris); /* Sombra suave */

    }

    h2{
        margin-top: 24px;
        font-weight: 300;
          font-size: 32px; /* Tamaño del texto responsivo */
          letter-spacing: 2px;
          line-height: 1.5;
    }
/* Estilos para el modal */
.modal {
      display: none; /* Escondido por defecto */
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.9); /* Fondo oscuro semitransparente */
      justify-content: center;
      align-items: center;
      z-index: 1000;
    }

    .modal img {
      max-width: 80%;
      max-height: 80%;
      object-fit: contain; /* Asegura que la imagen esté completamente visible */
      transition: transform 0.3s ease; /* Transición suave para el zoom */
    }

    /* Botón para cerrar */
    .close {
      position: absolute;
      top: 10px;
      right: 20px;
      color: white;
      font-size: 30px;
      cursor: pointer;
    }

    /* Agregar estilo para zoom con clases específicas */
    .zoom-in {
  transform: scale(1.5); /* Zoom in (agrandar) */
  transition: transform 0.3s ease; /* Transición suave */
}

.zoom-out {
  transform: scale(1); /* Volver al tamaño normal */
  transition: transform 0.3s ease; /* Transición suave */
}


@media (max-width: 600px) {


    .plano, .transporte{
        display: flex;
        justify-content: center;
        align-content: center;
        align-items: center;
        margin-bottom: 8vw;
        box-shadow: none;
    }

    .item1{
        max-width: 100vw;
        height: auto;
        margin-bottom: 8vw;
        box-shadow: none;
    }

    .accesos{
        margin-bottom: 8vw;
    }
}

/* Tableta: layout en 2 columnas */
@media (min-width: 601px) and (max-width: 1024px) {
    
.plano, .transporte{
        display: flex;
        justify-content: center;
        align-content: center;
        align-items: center;
        margin-bottom: 8vw;
        box-shadow: none;
    }

    .item1{
        max-width: 100vw;
        height: auto;
        margin-bottom: 8vw;
        box-shadow: none;
    }

    .accesos{
        margin-bottom: 8vw;
    }
}

@media (min-width: 1025px) {
    
    .plano, .transporte{
        display: flex;
        justify-content: center;
        align-content: center;
        align-items: center;
        margin-bottom: 4vw;
    }

    .item1{
        width: 100vw;
        height: auto;
        margin-bottom: 8vw;
    }

    .accesos{
        margin-bottom: 4vw;
    }
}

</style>

<script>
// Obtener los elementos necesarios
const img = document.getElementById('myImage') as HTMLImageElement;

const modal = document.getElementById('myModal');
const modalImage = document.getElementById('modalImage') as HTMLImageElement;
const closeModal = document.getElementById('closeModal');

let isZoomed = false;

// Al hacer clic en la imagen, mostrarla en el modal
img.onclick = function() {
  modal.style.display = 'flex'; // Mostrar el modal
  modalImage.src = img.src;     // Asignar la imagen al modal
};

// Al hacer clic en la 'X', cerrar el modal
closeModal.onclick = function() {
  modal.style.display = 'none'; // Ocultar el modal
  modalImage.classList.remove('zoom-in', 'zoom-out'); // Resetear el zoom
  isZoomed = false;
};

// Al hacer clic en la imagen en el modal, hacer zoom in/zoom out
modalImage.onclick = function() {
  if (!isZoomed) {
    modalImage.classList.add('zoom-in'); // Aplicar zoom-in
    isZoomed = true;
  } else {
    modalImage.classList.remove('zoom-in'); // Remover zoom-in
    modalImage.classList.add('zoom-out');   // Aplicar zoom-out
    isZoomed = false;
  }
};

  </script>